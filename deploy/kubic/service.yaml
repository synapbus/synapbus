apiVersion: v1
kind: Service
metadata:
  name: synapbus
  namespace: synapbus
  labels:
    app.kubernetes.io/name: synapbus
    app.kubernetes.io/instance: synapbus
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: synapbus
    app.kubernetes.io/instance: synapbus
  ports:
    - name: http
      port: 8080
      targetPort: http
      nodePort: 30088
      protocol: TCP
